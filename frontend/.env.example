# API base URL. Leave as /api to use the Vite dev proxy (see vite.config.ts).
# In production, point to the API origin (requires CORS) or serve same-origin.
VITE_API_BASE_URL=/api

# Dev-only: backend target the /api proxy forwards to.
VITE_PROXY_TARGET=http://localhost:8080
